# App URL for the sanity check (where Puppeteer navigates).
# Default: http://localhost:3000 (direct dev: pnpm run dev)
# With nginx: http://app.local-dev.com (after pnpm run nginx:start and hosts configured)
# SANITY_APP_URL=http://localhost:3000
# SANITY_APP_URL=http://app.local-dev.com

# Set to false to see the browser window (debug).
# HEADLESS=false

# Pause between steps in ms (default 1500). Set to 0 for no delay.
# SANITY_STEP_DELAY=2000
